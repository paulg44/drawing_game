// import { useSoundSettings } from "../context/SoundContext";

export default function Settings() {
  return (
    <div className="settingsPage">
      <button>
        {/* FX {isSoundEnabled ? "Sound ON" : "Sound OFF"} */}
        FX
      </button>
      <button>Sound</button>
    </div>
  );
}
